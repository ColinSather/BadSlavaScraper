create table events (
    EventID integer PRIMARY KEY,
    EventDate TIMESTAMP,
    Name text,
    Venue text,
    Address text,
    City text,
    State text,
    Frequency text,
    Cost numeric,
    Info text,
    Email text,
    Link text,
    Phone text,
    Reviews text
);
    
